Algoritmo de Hierholzer: Linear e muito eficiente
Complexidade de Tempo: O(E).

Complexidade de Espaço: O(V + E).

Algoritmo de Fleury:
Complexidade de Tempo: O(V²) = O(N²).

Complexidade de Espaço: O(V + E).

